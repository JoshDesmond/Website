{
  "name": "backend",
  "version": "1.0.0",
  "description": "A backend service for the website",
  "main": "backend.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	"run": "node backend.js >> logs.txt"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.3",
    "nodemailer": "^6.6.5"
  }
}
